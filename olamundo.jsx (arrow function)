import react, {component} from 'react'

 fEXPORT DEFAULT()={
 const informação = 'primeiro componente jsx'
 return(
 <div>
  <h2>componente olamundo</h2>
  <p> {informaçao} </p>
  </div>
  )
 }
